"""Presentation layer — FastAPI routers, request/response handling.

Contains: API routes, WebSocket handlers, streaming endpoints.
Must not contain business logic — delegates to use_cases layer.
"""
