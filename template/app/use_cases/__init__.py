"""Use Cases layer — LangGraph workflows and business orchestration.

Contains: agent graphs, workflow nodes, orchestration logic.
Depends on domain (schemas) and infrastructure (LLM, tools, DB).
"""
